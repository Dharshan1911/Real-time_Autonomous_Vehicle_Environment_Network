# RAVEN Environment Configuration Example

# Optional: Path to the UCI-HAR Dataset (if not at repository root)
# RAVEN_UCI_HAR_PATH=/path/to/RAVEN/data/external/UCI-HAR_Dataset

# API Server Port (default: 8000)
# PORT=8000

# Backend host (default: 0.0.0.0)
# HOST=0.0.0.0
